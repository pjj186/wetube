extends layouts/main
include mixins/videoBlock
//- videos 는 home에서 받아온다.
block content
    div.videos
        each item in videos
            +videoBlock({
                id:item.id,
                title: item.title,
                views: item.views,
                videoFile:item.videoFile
            })